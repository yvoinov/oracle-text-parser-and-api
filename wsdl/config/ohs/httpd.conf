<IfModule mod_rewrite.c>
RewriteEngine on
# If NOT used Oc4jMount, uncomment and use this line in httpd.conf
# Dont forget to open port 8888 onto firewall.
# RewriteRule ^/wsdl/ctxapi$ http://%{SERVER_NAME}:8888/wsdl/ctxapi$1 [L,R]
</IfModule>